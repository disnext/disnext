export const config = {
  description: "Returns pong as a response",
  locale: {
    name: {
      "en-US": "Ping",
    },
    description: {
      "en-US": "Returns pong as a response",
    },
  },
  permissions: {
    dm: false,
  },
};

export const run = () => {
  console.log("ha");
};
